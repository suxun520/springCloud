<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ncu.mapper.SqlToEnumMapper">
    
    
    
    <insert id="add"   parameterType="SqlToEnum">
       insert  into   sqltoenum(type,name)   values(#{type},#{name})
    </insert>

</mapper>

